package Planner;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.util.UUID;

/**
 * Класс записи
 */
public class Note {
    public Note(UUID uuid, LocalDateTime dateTimeAddNote, LocalDate deadLine, Person person, Priority priority) {
    }
}
